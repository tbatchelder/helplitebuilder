# Custom syntax on top of markdown -- prettier's markdown formatter doesn't
# know about @tt/@pc blocks and could mangle the pipe-delimited lines.
# help/ and help-source/ don't need entries here: both are in .gitignore,
# which prettier's CLI already respects by default.
templates/help.md
examples/example.md

# Auto-generated by npm
package-lock.json
